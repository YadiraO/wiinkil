import { styled } from 'stitches.conf'

export const LandingPageRoot = styled('div', {
    width: '100vw',
    height: '100vh',
    display: 'block',
    position: 'relative',

    '& > span': {
        position: 'absolute',
    },
})
