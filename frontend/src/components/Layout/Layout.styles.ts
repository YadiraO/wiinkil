import { styled } from 'stitches.conf'

export const LayoutContainer = styled('main', {
    margin: 0,
    padding: 0,
    border: 'none',
    background: '$m_mate',
    width: '100vw',
    height: '100vh',
})

export const MainRoot = styled('div', {
    display: 'flex',
    flex: 1,
    padding: 0,
    overflowX: 'auto',
    backgroundColor: '$c_verde',
    width: '100%',
    height: '100%',
})
